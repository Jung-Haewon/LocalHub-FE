# LocalHub - Frontend

Vue 3 + Vite frontend scaffold for LocalHub.

Quick start:

```bash
npm install
npm run dev
```

Notes:
- Kakao Maps JS key must be added in `src/views/MapView.vue` replacing `YOUR_KAKAO_JS_KEY`.
- Backend API base is `http://localhost:8000/api` (see `src/services/api.js`).
